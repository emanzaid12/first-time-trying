on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:  
      - name: 'Testing Github action'
        run: pwd

      - uses: actions/checkout@v3  
      - name: "Login to Docker hub"
        run: docker login --username emansameh12 --password dckr_pat_fSQteM0HCBmWmNVyQuuaisSMYBg